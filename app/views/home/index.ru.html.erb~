<% title t('.title_html') %>
<p class="error"><%= flash[:error] %></p>
<p class="notice"><%= flash[:notice] %></p>
<%= @time %>
<p><%= t('.cert_count') %>: <%= @cert_count_all %>.</p>

<% if user_signed_in? %>
  <% if can? :create, Certificate %>
    <p><%= t('.user_cert_count') %>: <%= @cert_count_cur %>.</p>
  <% end %>
  <% if can? :show, Certificate %>
    <%= link_to(t('.show_certs'), certificates_url) %>
  <% end %>

  <% if can? :create, User %>
    <p><%= t('.user_count') %>: <%= @user_count_all %>.</p>
  <% end %>
  <% if can? :show, User %>
    <%= link_to(t('.show_users'), users_url) %>
  <% end %>

<% end %>
